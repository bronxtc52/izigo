'use client';
import React from 'react';
import WebAdminShell from '@/views/admin/web/WebAdminShell';

// Веб-админка (admin.izigo.adarasoft.com). Гейт токена — в layout.js; здесь — каркас разделов.
export default function AdminPage() {
    return <WebAdminShell />;
}
